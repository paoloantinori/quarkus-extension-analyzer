<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.paoloantinori</groupId>
    <artifactId>quarkus-extension-analyzer-parent</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>quarkus-extension-analyzer-maven-plugin</artifactId>
  <packaging>maven-plugin</packaging>

  <name>quarkus-extension-analyzer :: Maven plugin</name>
  <description>
    The standalone Maven-plugin shell: resolves the ApplicationModel itself
    (ChainedMavenWorkspaceReader, the TASK-9 reactor-resolution machinery) and
    delegates classification to the core. For CI sweep over any built Quarkus
    app without modifying its pom. See docs/REARCH-PLAN.md.
  </description>

  <dependencies>
    <!-- The self-contained runner (TASK-20): core + bootstrap resolver, relocated under our
         namespace so projects exposing their own Quarkus classes cannot collide. -->
    <dependency>
      <groupId>io.github.paoloantinori</groupId>
      <artifactId>quarkus-extension-analyzer-shaded</artifactId>
    </dependency>

    <!-- Mojo API + the Maven-session collaborators injected via @Component (RepositorySystem,
         RemoteRepositoryManager, SettingsDecrypter) used to build the BootstrapAppModelResolver. -->
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-plugin-api</artifactId>
      <version>${maven.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-core</artifactId>
      <version>${maven.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.maven.resolver</groupId>
      <artifactId>maven-resolver-api</artifactId>
      <version>${resolver.version}</version>
      <scope>provided</scope>
    </dependency>
    <!-- org.eclipse.aether.impl.RemoteRepositoryManager: injected into the mojo (@Component) and fed
         into MavenArtifactResolver.Builder, see AnalyzeMojo's field javadoc for why. -->
    <dependency>
      <groupId>org.apache.maven.resolver</groupId>
      <artifactId>maven-resolver-impl</artifactId>
      <version>${resolver.version}</version>
      <scope>provided</scope>
    </dependency>
    <!-- org.apache.maven.settings.crypto.SettingsDecrypter: same reason. -->
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-settings-builder</artifactId>
      <version>${maven.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.maven.plugin-tools</groupId>
      <artifactId>maven-plugin-annotations</artifactId>
      <version>${plugin-tools.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Pinned explicitly (rather than the Maven-core default-bound 3.13.1) because 3.13.1 is not
           present in the local repository and this environment must not reach out for extra
           artifacts beyond normal dependency resolution; 3.15.1 already is. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-plugin-plugin</artifactId>
        <version>${plugin-tools.version}</version>
      </plugin>
    </plugins>
  </build>
</project>
